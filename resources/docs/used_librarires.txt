## .jar used in the project
# Add then to the used library when setting up the IDE for the project
# Remember to update the list whenever you use another lib when you code
# List may and will be modified (preferably only add to it)
# Indicate the ones you end up taking off the list but preferably don't
# Specify it in your commits when you're adding/removing anything to this list

# items in the list are specified in the following way :
# MAIN_LIBRARY_NAME :
#   - subclass_name (, other subclasses depending on the 1rst sublclass)

# EXAMPLE :
# LWJGL :
#   - lwjgl (, natives-*)
#   => refers to the following .jars :  lwjgl.jar, lwjgl-natives-linux.jar,
#                                       lwjgl-natives-windows.jar, etc.....

LWJG :
    - lwjgl (, natives-*)
    - lwjgl-assimp (, natives-*)
    - lwjgl-glfw (, natives-*)
    - lwjgl-asimp (, natives-*)
    - lwjgl-openal (, natives-*)
    - lwjgl-opengl (, natives-*)
            uses matixes to do graphics which is quicker and more efficient
    - lwjgl-stb (, natives-*)

# Used to load Textures / Images
SLICK :
    - slick-util.jar

# Maths library
JOML :

REMOVED :
    -
